// Core
import React, { FC } from 'react';
import styled from 'styled-components';

interface TypeProps {}

const Container = styled.div``;

export const Button: FC<TypeProps> = () => {
    return (
        <Container>
            Button
        </Container>
    );
};
