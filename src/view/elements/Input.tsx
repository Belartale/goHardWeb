// Core
import React, { FC } from 'react';
import styled from 'styled-components';

interface TypeProps {}

const Container = styled.div``;

export const Input: FC<TypeProps> = () => {
    return (
        <Container>
            Input
        </Container>
    );
};

