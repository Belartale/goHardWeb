// Core
import React, { FC } from 'react';
import styled from 'styled-components';

interface TypeProps {}

const Container = styled.div``;

export const Card: FC<TypeProps> = () => {
    return (
        <Container>
            Card
        </Container>
    );
};

