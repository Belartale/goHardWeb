// Core
import React, { FC } from 'react';
import { Container } from './styles';

export const UnderlineText: FC = ({ children }) => {
    return (
        <Container>
            {children}
        </Container>
    );
};

