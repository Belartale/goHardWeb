// Core
import { Box, styled } from '@mui/material';

export const Container = styled(Box, {})(() => ({
    height:         '100%',
    display:        'flex',
    justifyContent: 'center',
    alignItems:     'center',
}));
