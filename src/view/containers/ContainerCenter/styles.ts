// Core
import { Box, styled } from '@mui/material';

export const Container = styled(Box, {})(({ theme }) => ({
    height: '100%',


    display:        'flex',
    justifyContent: 'center',
    alignItems:     'center',
}));
